% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{pool.sign}
\alias{pool.sign}
\title{Pool Timepoints of a Dynamic Signed Network}
\usage{
pool.sign(
  net,
  timepoints,
  tie.breaker = c("zero", "positive", "negative", "first", "last"),
  dual.sign = FALSE
)
}
\arguments{
\item{net}{A dynamic signed network of class `dynamic.sign`.}

\item{timepoints}{Either an integer specifying the number of equal-sized groups,
or a list of integer vectors specifying which timepoints belong to each group.
E.g., `list(c(1,2,3), c(4,5,6))` or `3` for three equal groups.}

\item{tie.breaker}{How to resolve ties when positive and negative counts are equal.
One of `"zero"` (default), `"positive"`, `"negative"`, `"first"`, `"last"`.}

\item{dual.sign}{Logical. Allow positive and negative edges simultaneously
between the same pair. Defaults to FALSE.}
}
\value{
A dynamic signed network of class `dynamic.sign` with one network per
  pooled group, or a `static.sign` network if only one group results.
}
\description{
Aggregates timepoints of a dynamic signed network into pooled periods using
majority-vote logic, with configurable tie-breaking.
}
